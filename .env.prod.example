# Variables d'environnement pour la production
# Copier ce fichier en .env et remplir les valeurs

# Application
APP_SECRET=CHANGE_ME_WITH_A_SECURE_SECRET

# Domaine (pour le certificat SSL automatique)
# Exemples:
#   SERVER_NAME=example.com        → HTTPS avec Let's Encrypt
#   SERVER_NAME=:80                → HTTP uniquement (pas de SSL)
#   SERVER_NAME=localhost          → Certificat auto-signé (dev)
SERVER_NAME=example.com

# PostgreSQL
POSTGRES_USER=app
POSTGRES_PASSWORD=CHANGE_ME_WITH_A_SECURE_PASSWORD
POSTGRES_DB=franken_hostinger
